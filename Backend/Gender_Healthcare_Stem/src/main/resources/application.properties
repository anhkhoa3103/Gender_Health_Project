# --- App name ---
spring.application.name=Gender_Healthcare_Stem

# --- PostgreSQL connection ---
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.gysekzktgxuqlbtseijb
spring.datasource.password=genderhealth!123

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CORS (n?u có c?u hình global filter) ---
# (?ã c?u hình trong SecurityConfig r?i nên có th? không c?n thêm ? ?ây)

# --- JWT config (cho login/register có b?o m?t sau này) ---
app.jwt.secret=your_jwt_secret_key
app.jwt.expirationMs=86400000

# --- Email (forgot password dùng th?t) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ejejanhd@gmail.com
spring.mail.password=umgl lkbv ddxo fspk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# --- Timeformat ---
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.serialization.write-dates-as-timestamps=false


